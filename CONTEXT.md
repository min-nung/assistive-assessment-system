# 輔具評估與補助試算系統

治療師在案家或機構進行輔具評估時使用的離線優先網頁應用程式。評估資料保存在使用者自己的瀏覽器中，並可備份至使用者自己的雲端硬碟。

## 語彙

### 評估資料

**個案**：
一位接受輔具評估的服務對象，以及其名下所有評估資料的集合。以名稱唯一識別。
_避免_：病患、客戶、使用者

**使用者**：
操作本系統的治療師。系統中只有一位使用者，其資料不與他人共享。
_避免_：個案、帳號

**評估模組**：
針對單一輔具類別的評估表單與其資料，例如輪椅、氣墊床、沐浴椅。一個個案可同時擁有多個評估模組的資料，彼此獨立，清除其一不影響其他。
_避免_：表單、分頁、區塊

**基本資料**：
身高、體重、病史、管路等不屬於任何單一評估模組的個案資訊。刻意獨立於評估模組之外，使清除模組資料時不會被連帶清空。

### 備份與同步

**快照**：
某一時刻全部個案資料的完整副本，是備份與上傳的最小單位。系統從不傳送部分資料——快照要嘛完整、要嘛不存在。
_避免_：備份檔、匯出檔、差異

**真相來源**：
瀏覽器的 localStorage。雲端保存的是快照副本，不是真相來源；雲端資料只在使用者明確選擇載入時才會取代本機資料。

**雲端備份**：
將快照自動保存至使用者自己的 Google Drive。目的是防止資料遺失與更換裝置時取回，不是多裝置即時同步，也不是與他人共享。
_避免_：同步、雲端硬碟、共享

**衝突**：
雲端快照比本機最後一次上傳更新的狀態，代表另一台裝置曾寫入資料。系統一律不自動合併衝突，改由使用者選擇保留哪一份。
_避免_：不一致、版本差異
